
 <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
    <title><%= car.name %></title>
  </head>
  <body>
      <h1><%= car.name %></h1>
      <nav>
        <ul>
          <li>
            <a href="/cars">Back to Cars</a>
          </li>
        </ul>
      </nav>
    <% if (car.isReadyToSell) { %>
        <p>This car is ready to sell!</p>
        <% } else { %>
        <p> This car is not ready to sell!</p>
       <% } %> 
       
       <form action="/cars/<%= car._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete <%= car.name %></button>
      </form>
     <a href="/cars/<%= car._id %>/edit">Edit <%= fruit.name  %></a>
      
 </body>
</html> 
